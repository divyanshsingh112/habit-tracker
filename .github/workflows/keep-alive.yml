name: Keep Render Backend Alive

on:
  schedule:
    - cron: '*/14 * * * *' # Run every 14 mins
  workflow_dispatch: # Allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -I https://habit-tracker-m9uw.onrender.com
